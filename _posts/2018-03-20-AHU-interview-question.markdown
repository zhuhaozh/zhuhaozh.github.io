---
layout: post
title:  "安大复试题总结"  
date:   2018-03-23 07:35:00 +0800
categories : 大学课程 安大复试
---
* content
{:toc}

## 数学（7）
**线性与非线性的区别**

**证明两个集合的基数相等**

**离散数学等价关系判断方法**

**离线数学中的偏序**
-自反性，反对称性，传递的

**离线数学子群**

**离线数学混合图、赋权图**

**离线数学通路、回路**

## 编程语言（23）
**指针和引用的区别**

**汇编**

**面向对象的特征？**
- 封装继承多态

**面向对象中多态的理解**
- 允许你将父对象设置成为一个或更多的他的子对象相等的技术，赋值之后，父对象就可以根据当前赋值给它的子对象的特性以不同的方式运作。

**java中有没有goto语句？goto语句的利弊**
- 没有，优点：操作灵活，缺点：破坏程序结构性

**内存中的堆、栈、静态区**
- https://www.cnblogs.com/heyanan/p/6125030.html
静态区：编译时就分配好内存，程序运行期间一直存在，存储静态数据，全局变量和常量
栈：在执行函数的时候函数内部的存储单元都可以在栈里面创建，函数结束就自动释放
堆：动态内存分配，用malloc和new申请，用free释放，生存周期由我们自己决定

**面向对象程序设计中重载与重写的区别与联系**
- 重载:两个方法方法名相同,参数不同,调用时,传入不同的参数执行对应的方法
- 重写:继承父类或实现接口时,对原有的父类函数重写的改写

**java中如何创建一个线程**
- 继承Thread类,重写run方法,调用start()
- 实现Runnable接口

**java中处理多线程相关的函数有哪些**
- run()/start()/join()/setDeamon()/sleep()/wait()/notify()

**java与c++处理内存上的区别**
- java:GC
- c++:手工

**虚函数**

**c++中的几种继承方式**
public基类中的公有成员和保护成员都作为派生类中的公有成员
**静态函数/静态变量？**
只能在本文件中使用，在其他源文件中不能使用但是可以定义同名的函数
静态变量一直存在与程序执行期间，函数退出时，变量里的值也不会释放

**堆栈溢出原因**
函数地柜深度太深
数组太大溢出
死循环，不断往堆栈中放数据
**c是不是结构化查询语言**
- 不是

**结构化程序有哪些构成？谈谈对其理解？**
- 顺序/选择/循环

**c语言中指针和指针变量**
- 指针:指向某个数据地址的变量
- 指针变量:指向指针地址的变量

**c语言中有几种运算符**
34个 10类 算术运算符，关系运算符>,<,=，逻辑运算符等

**编译语言和解释性语言的区别**
- 编译:编译器先将代码转换为对应的机器语言后执行,能够较好的优化,速度一般也更快与解释语言
- 解释:代码边运行边转换为对应的机器语言,更灵活但速度通常较慢

**指针数组、数组指针**
- 指针数组:多个指针组成的数组
- 数组指针:指向数组的指针

## 数据结构（27）
**什么是算法、算法的特点**
- 算法是解决特定问题求解步骤的描述，在计算机中表现为指令的有限序列，并且每条指令表示一个或多个操作。简单来说，算法就是解决问题的方式和方法。

- 特点:
1. 有穷性：依据图灵的定义，一个算法是能够被任何图灵完备系统模拟的一串运算，而图灵机器只有有限个状态、有限个输入符号和有限个转移函数（指令）。而一些定义更规定算法必须在有限个步骤内完成任务。
2. 确定性：算法的描述必须无歧义，以保证算法的实际执行结果是精确地符合要求或期望，通常要求实际运行结果是确定的。
3. 可行性。能够实现，算法中描述的操作都是可以通过已经实现的基本运算执行有限次来实现。
4. 输入：一个算法必须有零个或以上输入量。[1]
5. 输出：一个算法应有一个或以上输出量，输出量是算法计算的结果。

**数据结构中衡量算法优劣的指标**
- 时间复杂度、空间复杂度

**时间复杂度、空间复杂度**
时间复杂度描述了算法的运行时间，用O表示，不包括一些常数项，只包含问题的规模
空间复杂度定义一个算法所耗费的存储空间，用o表示
**图的邻接表邻接矩阵**
- 两种图的存储存储方式
- Arcs[][]用一个二维数组存放顶点间关系（边或弧）的数据，这个二维数组称为邻接矩阵。无向图的邻接矩阵是对称矩阵

- 邻接表，存储方法跟树的孩子链表示法相类似，是一种顺序分配和链式分配相结合的存储结构。如这个表头结点所对应的顶点存在相邻顶点，则把相邻顶点依次存放于表头结点所指向的单向链表中。

- 邻接表适用于稀疏图,邻接矩阵适用于稠密图

**链表有几种结构**
- 单链表
- 循环链表
- 双向链表
- 静态链表

**稳定、不稳定的排序算法**
- 稳定排序:待排序的记录序列中可能存在两个或两个以上关键字相等的记录,在排序后它们的相对位置依旧保持不变

**算法的控制方式有哪几种**

**顺序表和线性表的比较**
- 内存分配的不同:预分配(连续)/随机分配
- 时间性能的不同:查找/删除/添加/修改


**单向联通、弱连通、强连通？**
- 设D是一个有向图，如果D中任意两个结点都彼此可达，则称D为强*连通图*。
- 如果D中任意两点u,v之间，有u->v可达或v->u可达（称为单向可达），则称D为*单向连通图*。
- 如果有向图的底图是无向连通图，则称D为*弱连通图*。

**双向链表如何逆置**
```
while (cur)  
{  
    std::swap(cur->_next, cur->_prev);  
    cur = cur->_next;  
}
```


**折半查找算法**

**二叉树的遍历**

**中序遍历**

**b树？**

**红黑树特点**

**平衡二叉树**

**关键路径**

**最短路径**

**模式匹配**

**dijkstra算法**

**拓扑排序的基本算法**

**归并排序算法**

**贪心算法**

## 硬件知识（9）
点位和电压的区别
二极管是？
8253的编程方式
场效应管
与非门输入两个0，输出什么
数字逻辑中顺序结构化程序
数字逻辑中的最优状态
模电中的pn结
上拉电阻、下拉电阻的作用？

## 计算机组成（23）
**CPU有那些寄存器**
数据寄存器：向内存中存入一条数据时，也将他们暂时存放在此
地址寄存器：用来存放当前cpu只在访问单元的地址
累加寄存器：用来存放一个逻辑单元的运算结果
程序计数器：用来控制地址以便程序正确运行
指令寄存器，用来保存正在执行的一条指令
状态寄存器：保存各种状态标志

**指令执行的阶段与任务**
- 取指/间指/执指/中断
取值：从程序存储器中取出指令
分析指令：将指令寄存器中的指令取出译码，分析指令性质
执行指令；

**冯诺依曼结构的特征**
1. 由运算器、存储器、控制器、输入设备、输出设备组成
2. 指令和数据以同等地位存放存储器中，按地址寻址
3. 指令和数据以二进制的形式表示
4. 指令由操作码和地址码组成
5. 指令在存储器内按顺序存放
6. 在经典的冯诺依曼结构中以运算器为核心

**计算机硬件组成，电脑的主要硬件有哪些**
- 运算器、存储器、控制器、输入设备、输出设备

**DMA接口的组成部分**
-

**中断产生的原因**
外部中断，cpu外的原因引起的程序中断
内部中断：内部执行程序错误引起的中断
  除法错中断
  指令中断
  溢出中断
  单步中断


**超加法器**
对普通加法器进行改良的并行加法器，对普通串行加法器的相互进位的延迟进行了改良。
**说出几种存储器的特点**
- 按地址访问随机存取

**I/O接口**
主机与被控对象进行信息交换的纽带，主机通过i/o接口与外界设备进行数据交换，绝大多数的i/o接口都是可编程的，他们的工作方式可以由程序进行控制
**比较RISC和CISC**
1. 充分利用VLSI芯片
2. 提高运算速度
3. 便于设计，可降低成本，提高可靠性
4. 有效支持高级语言程序
5. RISC的兼容性较差

**寻址方式**
- 直接
- 立即数
- 间接
- 寄存器直接
- 寄存器间接
- 相对
- 变址
- 基址


**cpu的组成部分与其功能**
控制器：从内存中取出一条指令并指出下一条指令在内存中的位置
运算器：逻辑运算
寄存器：暂存指令数据地址


**cpu与外设交换信息的方式**
- 程序控制方式
- 程序中断
- DMA
- 通道

**dma、中断查询、程序查询？**

**cache的映射方式**
- 全相联映射
- 直接映射
- 组相联映射

**cpu响应中断的条件**
- 开中断
- 当前中断优先级最高
- 一条指令周期执行结束


**存取周期和存取时间**
- 存取周期：两次访存的最小时间间隔
- 存取时间：地址稳定到读取或写入一个数据的时间

**dram为什么要刷新**Dynamic Random Access Memory动态随机存取存储器，
- 电容的电会逐渐消失

**微命令是什么？微命令、微指令、微操做、微程序的比较与理解？**
- 微命令：控制部件通过控制线，向执行部件发送控制命令

**arm？谈谈理解**

**高速缓存的原理**
- 缓解cpu与io速度不匹配的问题
- 时间局部性、空间局部性

**存储器带宽**

**数据线、地址线的理解**

## 操作系统（28）
**如何预防死锁**

**os的特征、基本特征**
- 并发共享虚拟异步

**PCB**
- 进程控制块

**分页与分段的区别**
- 分页：页的大小是固定的、为了满足系统需要、一维

- 分段：段的大小不固定、为了满足用户的需要、二维

  **计算机系统的异步性**

**操作系统进程的三种不同状态**
- 就绪、运行、阻塞

**页表需要哪些支持**
- 页表寄存器
- 缺页中断机构
- 地址变换机构

**通道的功能**

**作业调度静态优先级和动态优先级**
- 静态：用户提前设置好，不再改变，饥饿现象
- 动态：高响应比调度，响应比 RP=响应时间/服务时间


**银行家算法的思想**

**虚拟寄存器特性**

**OS有哪些将程序装入内存的方法**

**实现重定位需要哪些硬件**

**死锁是什么**
- 系统中的进程竞争资源的顺序不当，使得系统中的进程在没有外力作用的情况下，无法继续执行下去

**死锁的避免、预防、必要条件**
- 必要条件：互斥、请求保持、不可剥夺、环路等待
- 预防：
  1. 一次分配全部的资源
  2. 在系统运行的过程中，进程拥有的资源可被剥夺
  3. 给资源加上序号，按序分配
- 避免：银行家算法

**举例说明同步与互斥**
- 同步：生产者消费者
- 互斥：打印机

**管程是什么**
- 将不同位置的wait();signal();语句集中起来管理

**虚拟存储器？**

**缓冲的定义、为什么要缓冲 **
- 在CPU与主存之间的一级高速存储器，将CPU需要访问的资源暂存到缓存上
- 缓解CPU与主存速度不匹配的问题

**多道程序技术的作用**
- 提高机器的使用效率

**管态？目态？**
- 管态：系统态
- 目态：用户态

**父子进程是否可以并发运行**
- 可以

**库函数和系统调用的区别与联系**
- 库是可重用的模块 处于用户态
- 系统调用是os提供的服务 处于内核态 不能直接调用 而要使用类似int 0x80的软中断陷入内核 所以库函数中有很大部分是对系统调用的封装

**可封闭性和可再现性**
- 可封闭性：多个程序不共享各种资源，每个程序在执行的时候，不能使用其他程序正在使用的资源
- 可再现性：一个程序的多次调用在数据保持不变的情况下，结果不变

**分时和实时系统**
- 分时操作系统：使一台计算机同时为几个、几十个甚至几百个用户服务的一种操作系统。把计算机与许多终端用户连接起来，分时操作系统将系统处理机时间与内存空间按一定的时间间隔，轮流地切换给各终端用户的程序使用。由于时间间隔很短，每个用户的感觉就像他独占计算机一样。分时操作系统的特点是可有效增加资源的使用率。交互性是其最大的特点。
- 实时操作系统：必须能在一定时间限制内解决实时任务，这些任务通常与某个外部设备有关，能反应或控制相应的外部设备，具有一定紧迫性。可分为硬实时、软实时

**作业与作业步**
- 作业由不同的顺序相连的作业步组成。作业步是在一个作业的处理过程中，计算机所做的相对独立的工作。
- 一般来说，每一个作业步产生下一个作业步的输入文件。

**程序和进程的关系**
- 进程是程序及其数据在计算机的一次运行活动，是一个运行过程，是一个动态的概念。进程的运行实体是程序，离开程序的进程没有存在的意义。而程序是一组有序的指令集合，是一种静态概念。
- 进程是程序的一次执行过程，它是动态地创建和消亡的，具有一定的生命周期，是暂时存在的；而程序则是一组代码的集合，它是永久存在的，可长期保存。
- 一个进程可以执行一个或几个程序，一个程序也可以构成多个进程。进程可以创建进程，而程序不能形成新的程序。
- 进程和程序的组成不同。从静态角度看，进程由程序、数据和进程控制块（PCB）三部分组成。而程序是一组有序的指令集合。

## 软件工程（10）
**软件的生命周期**
- 问题的定义及规划
- 需求分析
- 软件设计
- 程序编码
- 软件测试
- 运行维护

**软件工程的三要素**
- 方法 工具 过程

**软件工程定义**
- 软件工程是研究和应用如何以系统性的、规范化的、可定量的过程化方法去开发和维护软件，以及如何把经过时间考验而证明正确的管理技术和当前能够得到的最好的技术方法结合起来。

**大型软件的测试步骤**

**瀑布模型**

**软件测试有哪些步骤**

**软件测试的阶段划分**

**白盒与黑盒测试的区别**

**软件开发模型？理解？**

**回归测试？**

## 网络（40）
**BGP协议**
- 边界网关协议（BGP）是运行于 TCP 上的一种自治系统的路由协议。 BGP 是唯一一个用来处理像因特网大小的网络的协议，也是唯一能够妥善处理好不相关路由域间的多路连接的协议。 BGP 构建在 EGP 的经验之上。 BGP 系统的主要功能是和其他的 BGP 系统交换网络可达信息。网络可达信息包括列出的自治系统（AS）的信息。这些信息有效地构造了 AS 互联的拓朴图并由此清除了路由环路，同时在 AS 级别上可实施策略决策。
网络是由一组AS系统（自治域）系统组成的，那么在一个AS里的路由器用的选路协议就是内部网关协议IGP，不同AS的路由器所用的选路协议就是外部网关协议EGP,BGP属于外部网关协议

**tcp与udp比较**
- TCP与UDP基本区别
TCP（Transmission ControlProtocol，传输控制协议）是面向连接的协议，也就是说，在收发数据前，必须和对方建立可靠的连接。一个TCP连接必须要经过三次“对话”才能建立起来，
UDP协议全称是用户数据报协议[1]  ，在网络中它与TCP协议一样用于处理数据包，是一种无连接的协议。
  1. 基于连接与无连接
  2. TCP要求系统资源较多，UDP较少；
  3. UDP程序结构较简单
  4. 流模式（TCP）与数据报模式(UDP);
  5. TCP保证数据正确性，UDP可能丢包
  6. TCP保证数据顺序，UDP不保证
　　
- UDP应用场景：
  1. 面向数据报方式
  2. 网络数据大多为短消息
  3. 拥有大量Client
  4. 对数据安全性无特殊要求
  5. 网络负担非常重，但对响应速度要求高


**ip与物理地址作用在哪一层**
- IP:网络层
- 物理地址：物理层

**以太局域网第二层可靠吗**

**IP编址的几种方式**
1. 分类IP编址：ABCD
2. 子网掩码编址：
3. 无分类编址CIDR

**网络协议的三个要素以及其概念与作用**
- 语法，定义了数据与控制信息的格式;
- 语义，定义了需要发出何种控制信息，完成何种响应动作以及作出何种响应;
- 同步，定义了事件实现顺序的详细说明;

**ip协议配套的其他协议？**
- 网络层：为数据包选择路由
- 协议有：IP，ICMP，OSPF，EIGRP，IGMP

**tcp/ip协议的核心是什么（everything over ip）**

**arp协议**
- 地址解析协议，即ARP（Address Resolution Protocol），是根据IP地址获取物理地址的一个TCP/IP协议。主机发送信息时将包含目标IP地址的ARP请求广播到网络上的所有主机，并接收返回消息，以此确定目标的物理地址；收到返回消息后将该IP地址和物理地址存入本机ARP缓存中并保留一定时间，下次请求时直接查询ARP缓存以节约资源。

**icmp协议**
- ICMP是（Internet Control Message Protocol）Internet控制报文协议。它是TCP/IP协议族的一个子协议，用于在IP主机、路由器之间传递控制消息。控制消息是指网络通不通、主机是否可达、路由是否可用等网络本身的消息。这些控制消息虽然并不传输用户数据，但是对于用户数据的传递起着重要的作用。
错误侦测与回报机制，ping命令就是icmp的一个协议工作过程

**tftp 协议**
- TFTP（Trivial File Transfer Protocol,简单文件传输协议）是TCP/IP协议族中的一个用来在客户机与服务器之间进行简单文件传输的协议，提供不复杂、开销不大的文件传输服务。端口号为69。基于udp协议，不可靠，ftp的简化，ftp基于tcp，可靠

**http协议**
- 超文本传输协议（HTTP，HyperText Transfer Protocol)是互联网上应用最为广泛的一种网络协议。所有的WWW文件都必须遵守这个标准。设计HTTP最初的目的是为了提供一种发布和接收HTML页面的方法
规定浏览器与万维网服务器之间的通信规则

**dhcp协议**
- DHCP（Dynamic Host Configuration Protocol，动态主机配置协议）是一个局域网的网络协议，使用UDP协议工作， 主要有两个用途：给内部网络或网络服务供应商自动分配IP地址，给用户或者内部网络管理员作为对所有计算机作中央管理的手段

**网桥的作用**
- 网桥（Bridge）是早期的两端口二层网络设备，用来连接不同网段。网桥的两个端口分别有一条独立的交换信道，不是共享一条背板总线，可隔离冲突域。网桥比集线器（Hub）性能更好，集线器上各端口都是共享同一条背板总线的。后来，网桥被具有更多端口、同时也可隔离冲突域的交换机（Switch）所取代。
工作在数据链路层

**路由器的功能和原理**
- 路由器（Router），是连接因特网中各局域网、广域网的设备，它会根据信道的情况自动选择和设定路由，以最佳路径，按前后顺序发送信号。 路由器是互联网络的枢纽，"交通警察"。目前路由器已经广泛应用于各行各业，各种不同档次的产品已成为实现各种骨干网内部连接、骨干网间互联和骨干网与互联网互联互通业务的主力军。
- 路由和交换机之间的主要区别就是交换机发生在OSI参考模型第二层（数据链路层），而路由发生在第三层，即网络层。这一区别决定了路由和交换机在移动信息的过程中需使用不同的控制信息，所以说两者实现各自功能的方式是不同的。
为经过路由器的每个数据包寻找一条最佳路径 路由表，静态路由表，动态路由表

**防火墙的端口防护是指什么**
- 指通过对防火墙的端口开关的设置，关闭一些非必需端口，达到一定安全防护目的行为
对某一个端口进行基于ip地址，方向，关键字等的检查过滤，判断是否准入或准出，以保护防火墙内的系统

**网络按地域范围的划分**
- 局域网（lan）：一般限定在较小的区域内，小于10km的范围，通常采用有线的方式连接起来。
- 城域网（man）：规模局限在一座城市的范围内，10～100km的区域。
- 广域网（wan）：网络跨越国界、洲界，甚至全球范围。

**网络按使用者分类**
公用网 和专用网

**网络的拓扑结构有哪些**
- 网络拓扑是网络形状，或者是网络在物理上的连通性。网络拓扑结构是指用传输媒体互连各种设备的物理布局，即用什么方式把网络中的计算机等设备连接起来。拓扑图给出网络服务器、工作站的网络配置和相互间的连接。
- 网络的拓扑结构有很多种，主要有星型结构、环型结构、总线结构、分布式结构、树型结构、网状结构、蜂窝状结构等。

**计算机网络体系结构**
- 计算机网络体系结构可以从网络体系结构、网络组织、网络配置三个方面来描述，网络组织是从网络的物理结构和网络的实现两方面来描述计算机网络，网络配置是从网络应用方面来描述计算机网络的布局，硬件、软件和通信线路来描述计算机网络，网络体系结构是从功能上来描述计算机网络结构。

**什么是网络延迟？为什么出现？**
- 一个信号在它的发送和它的最后接受之间存在一个延迟。每个网络都受这个延迟的支配。例如，当你在计算机上敲一个键将一个文件保存到网络上时，文件的数据在它到达服务器的硬盘时必须通过网络接口卡、网络中的一个集线器或是一个交换机或路由器、更多的电缆以及服务器端的网络接口卡。虽然电子传输迅速，但它们仍然不得不经过传输这一过程。这个过程在你敲键的那一刻和服务器接收数据的那一刻之间必然存在短暂的延迟，这种延迟被称为时延。
同时，当连接多个网络段时，也将增加网络上的时延。为了限制时延并避免相关的错误，每种类型的介质都标定一个最大连接段数。

**三次握手协议**
- 三次握手协议指的是在发送数据的准备阶段，服务器端和客户端之间需要进行三次交互：第一次握手：客户端发送syn包(syn=j)到服务器，并进入SYN_SEND状态，等待服务器确认；第二次握手：服务器收到syn包，必须确认客户的syn（ack=j+1），同时自己也发送一个SYN包（syn=k），即SYN+ACK包，此时服务器进入SYN_RECV状态；第三次握手：客户端收到服务器的SYN+ACK包，向服务器发送确认包ACK(ack=k+1)，此包发送完毕，客户端和服务器进入ESTABLISHED状态，完成三次握手。连接建立后，客户端和服务器就可以开始进行数据传输了。

**snmp**
- 简单网络管理协议（SNMP），由一组网络管理的标准组成，包含一个应用层协议（application layer protocol）、数据库模型（database schema）和一组资源对象。该协议能够支持网络管理系统，用以监测连接到网络上的设备是否有任何引起管理上关注的情况。该协议是互联网工程工作小组（IETF，Internet Engineering Task Force）定义的internet协议簇的一部分。SNMP的目标是管理互联网Internet上众多厂家生产的软硬件平台，因此SNMP受Internet标准网络管理框架的影响也很大。SNMP已经出到第三个版本的协议，其功能较以前已经大大地加强和改进了。

**码元是？码元的长度？**
- 在数字通信中常常用时间间隔相同的符号来表示一个二进制数字
- 这个间隔被称为码元长度。

**说出计算机网络中的三种协议，选其一具体说明**
- 见上

**防火墙的原理**---------------------------------------------------
1. 包过滤技术
 - 包过滤是最早使用的一种防火墙技术，它的第一代模型是“静态包过滤”（Static Packet Filtering），使用包过滤技术的防火墙通常工作在OSI模型中的网络层（Network Layer）上，后来发展更新的“动态包过滤”（Dynamic Packet Filtering）增加了传输层（Transport Layer），简而言之，包过滤技术工作的地方就是各种基于TCP/IP协议的数据报文进出的通道，它把这两层作为数据监控的对象，对每个数据包的头部、协议、地址、端口、类型等信息进行分析，并与预先设定好的防火墙过滤规则（Filtering Rule）进行核对，一旦发现某个包的某个或多个部分与过滤规则匹配并且条件为“阻止”的时候，这个包就会被丢弃。适当的设置过滤规则可以让防火墙工作得更安全有效，但是这种技术只能根据预设的过滤规则进行判断，一旦出现一个没有在设计人员意料之中的有害数据包请求，整个防火墙的保护就相当于摆设了。
2. 应用代理技术
 - 由于包过滤技术无法提供完善的数据保护措施，而且一些特殊的报文攻击仅仅使用过滤的方法并不能消除危害（如SYN攻击、ICMP洪水等），因此人们需要一种更全面的防火墙保护技术，在这样的需求背景下，采用“应用代理”（Application Proxy）技术的防火墙诞生了。我们的读者还记得“代理”的概念吗？代理服务器作为一个为用户保密或者突破访问限制的数据转发通道，在网络上应用广泛。我们都知道，一个完整的代理设备包含一个服务端和客户端，服务端接收来自用户的请求，调用自身的客户端模拟一个基于用户请求的连接到目标服务器，再把目标服务器返回的数据转发给用户，完成一次代理工作过程。那么，如果在一台代理设备的服务端和客户端之间连接一个过滤措施呢？这样的思想便造就了“应用代理”防火墙，这种防火墙实际上就是一台小型的带有数据检测过滤功能的透明代理服务器（Transparent Proxy），但是它并不是单纯的在一个代理设备中嵌入包过滤技术，而是一种被称为“应用协议分析”（Application Protocol Analysis）的新技术。
3. 　这是继“包过滤”技术和“应用代理”技术后发展的防火墙技术，它是CheckPoint技术公司在基于“包过滤”原理的“动态包过滤”技术发展而来的，与之类似的有其他厂商联合发展的“深度包检测”（Deep Packet Inspection）技术。这种防火墙技术通过一种被称为“状态监视”的模块，在不影响网络安全正常工作的前提下采用抽取相关数据的方法对网络通信的各个层次实行监测，并根据各种过滤规则作出安全决策。

**ip组播的好处？**
- IP组播（IP　multicasting）是对硬件组播的抽象，是对标准IP网络层协议的扩展。它通过使用特定的IP组播地址，按照最大投递的原则，将IP数据包传输到一个组播群组（multicast　group）的主机集合。它的基本方法是：当某一个人向一组人发送数据时，它不必将数据向每一个人都发送数据，只需将数据发送到一个特定的预约的组地址，所有加入该组的人均可以收到这份数据。这样对发送者而言，数据只需发送一次就可以发送到所有接收者，大大减轻了网络的负载和发送者的负担
- 组播可以增强报文发送效率，控制网络流量，减少服务器和CPU负载；
- 组播可以优化网络性能，消除流量冗余；
- 组播可以适应分布式应用，当接收者数量发生变化时，网络流量的波动很平稳。

**路由器工作在哪个层**
- 网络层

**局域网？**
- 局域网（Local Area Network，LAN）是在一个局部的地理范围内（如一个学校、工厂和机关内），一般是方圆几千米以内，将各种计算机，外部设备和数据库等互相联接起来组成的计算机通信网。它可以通过数据通信网或专用数据电路，与远方的局域网、数据库或处理中心相连接，构成一个较大范围的信息处理系统。

**举例说明tcp和udp适用环境**
- http://blog.csdn.net/u013777351/article/details/49226101

**交换机与路由器，谈谈两者在网络中的作用和工作原理**
- 交换机
 - 用于同一网络内部数据的快速传输
 - 转发决策通过查看二层头部完成
 - 转发不需要修改数据帧
 - 工作在 TCP/IP 协议的二层 —— 数据链路层
 - 工作简单，直接使用硬件处理
- 路由器
 - 用于不同网络间数据的跨网络传输
 - 转发决策通过查看三层头部完成
 - 转发需要修改 TTL ，IP 头部校验和需要重新计算，数据帧需要重新封装
 - 工作在 TCP/IP 协议的三层 —— 网络层
 - 工作复杂，使用软件处理
- 三层交换机
 - 三层(多层)交换机的路由引擎较弱，相比路由器支持的路由协议有限。
 - 同样由于三层(多层)交换机的路由引擎较弱，且转发表有限，不适合复杂的多网络互联
 - 因为并不是每一个包都会经过路由引擎，三层(多层)交换机的路由策略功能较弱。
 - 三层(多层)交换机更适合网络结构简单，交换数据量大的情景下使用（例如小型网络的汇聚层）路由器更适合复杂的多网络互联的核心交换区域。

- 工作层次不同：
 - 交换机主要工作在数据链路层（第二层）
 - 路由器工作在网络层（第三层）。
- 转发依据不同：
 - 交换机转发所依据的对象时：MAC地址。（物理地址）
 - 路由转发所依据的对象是：IP地址。（网络地址）
- 主要功能不同：
 - 交换机主要用于组建局域网，
 - 而路由主要功能是将由交换机组好的局域网相互连接起来，或者接入Internet。
 - 交换机不能分割广播域，路由可以。
 - 路由还可以提供防火墙的功能。
 - 路由配置比交换机复杂。

**dns？谈谈理解**
- DNS（Domain Name System，域名系统），万维网上作为域名和IP地址相互映射的一个分布式数据库，能够使用户更方便的访问互联网，而不用去记住能够被机器直接读取的IP数串。通过域名，最终得到该域名对应的IP地址的过程叫做域名解析（或主机名解析）。

**ppp协议**
- 点对点协议（PPP）为在点对点连接上传输多协议数据包提供了一个标准方法。PPP 最初设计是为两个对等节点之间的 IP 流量传输提供一种封装协议。在 TCP-IP 协议集中它是一种用来同步调制连接的数据链路层协议（OSI 模式中的第二层），替代了原来非标准的第二层协议，即 SLIP。除了 IP 以外 PPP 还可以携带其它协议，包括 DECnet 和 Novell 的 Internet 网包交换（IPX）。

**子网掩码的作用**
- 子网掩码(subnet mask)又叫网络掩码、地址掩码、子网络遮罩，它是一种用来指明一个IP地址的哪些位标识的是主机所在的子网，以及哪些位标识的是主机的位掩码。子网掩码不能单独存在，它必须结合IP地址一起使用。子网掩码只有一个作用，就是将某个IP地址划分成网络地址和主机地址两部分。
子网掩码是一个32位地址，用于屏蔽IP地址的一部分以区别网络标识和主机标识，并说明该IP地址是在局域网上，还是在远程网上。

## 数据库（35）
**数据库管理系统**

**1NF是什么，数据模式是什么**
- 1NF：关系中的属性都是不可再分的
- 数据模式：

**关系数据库有哪些操作与其特点**
- 增删改查

**数据独立性**
- 逻辑独立性
- 物理独立性

**数据恢复**
- 数据转储和登记日志文件

**关系运算**
- 专门：选择投影连接除
- 基本：并、差、积、选、投

**三个数据模型**
- 层次、网状、关系

**数据的不一致性**

**数据独立性**
- 逻辑独立性、物理独立性

**如何实现逻辑独立性和物理独立性**
- 两层映像

**select语句有哪些功能**
- 投影

**数据库设计步骤**
- 需求分析
- 概念结构设计
- 逻辑结构设计
- 物理结构设计
- 数据库实施
- 运行和维护

**数据库计算的分类**
- ？？？

**什么是完整性约束**
- 为了保证数据库的完整性
- 实体、参照、自定义


**sql的四个组成部分**
- 数据查询语言DQL
- 数据操纵语言DML
- 数据定义语言DDL
- 数据控制语言DCL

**dbms支持哪几种数据模型**
- 层次、网状、关系

**常见的应用数据库**
- mysql、sqlserver、oracle、db2

**数据库操纵语言举例**

**基本表？视图？区别与联系**

**视图表的作用**
- 简化用户的操作
- 同一种数据可以向用户提供不同的角度查看
- 提供了一定的安全性保护
- 提高了数据库的逻辑独立性
- 可以向用户提供更清晰的查询方式

**数据库的三级模式结构**
- 外模式、模式、内模式

**一个数据库的组成和特点**
- DBMS、DB、DBA、OS

**er图的三个基本关系**
- 1:1/1:m/n:m

**n：n联系的解释与举例**
- 学生选课
- 订单与商品

**数据转储**
- 动态、静态
- 海量、增量

**数据恢复的方法**
- 数据转储
- 登记日志文件

**二元关系有哪些表示方法**
- 1:1/1:n/n:m/

**sql注入攻击是什么？怎么预防**

**mysql与oracle分页上的区别**
- MySQL：limit startNum,totalNum
- oracle: 使用rownum？？

**sql中索引所用的数据结构类型**
- hash
- B树

## 编译原理（8）
**范式**

**语法分析**

**编译原理的阶段与任务**

**什么是单词**

**什么是前缀与后缀**

**析取范式、主析取范式**

**静态语义检查**

**确定有穷自动机**

## 其他（10）
**中国第一个研究计算机的研究所？**

**最有效的方法计算2×8**
- 左移3位

**你了解哪几种启发式算法？**
- 启发式算法：一个基于直观或经验构造的算法，在可接受的花费（指计算时间和空间）下给出待解决组合优化问题每一个实例的一个可行解，该可行解与最优解的偏离程度一般不能被预计。
- 蚁群算法、模拟退火法、神经网络

**蚁群算法、遗传算法、模拟退火算法、蜂群算法了解几个？谈谈理解**
- 蚁群算法基本思路为：用蚂蚁的行走路径表示待优化问题的可行解，整个蚂蚁群体的所有路径构成待优化问题的解空间。路径较短的蚂蚁释放的信息素量较多，随着时间的推进，较短的路径上累积的信息素浓度逐渐增高，选择该路径的蚂蚁个数也愈来愈多。最终，整个蚂蚁会在正反馈的作用下集中到最佳的路径上，此时对应的便是待优化问题的最优解。

**图灵是谁？谈谈对他的理解**

**cff是什么**

**acm是什么**

**exe文件和com文件的区别**

**ipconfig的作用**

**大数据的理解**
